{
  "name": "playback-mcp",
  "private": true,
  "license": "MIT",
  "author": "Blaise Moses <bamoses2001@gmail.com>",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/BlaiseMoses01/playback-mcp.git"
  },
  "homepage": "https://blaisemoses01.github.io/playback-mcp/",
  "bugs": "https://github.com/BlaiseMoses01/playback-mcp/issues",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm -r build",
    "lint": "oxlint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc --noEmit -p server/tsconfig.json && tsc -p extension/tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "prepare": "husky",
    "smoke": "node scripts/mcp-poke.mjs"
  },
  "lint-staged": {
    "*.{ts,js,mjs}": [
      "oxlint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^17.1.0",
    "oxlint": "^1.75.0",
    "oxlint-tsgolint": "^7.0.2001",
    "prettier": "^3.9.6",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
