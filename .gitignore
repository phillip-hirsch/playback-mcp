# Dependencies
node_modules/
# npm lockfile — this repo uses pnpm (pnpm-lock.yaml)
package-lock.json

# Build output (server/dist, extension/dist)
dist/

# Packaging artifacts
*.zip
*.tgz

# Test/coverage output
coverage/

# Logs
*.log
npm-debug.log*

# Environment / secrets
.env
.env.*

# OS cruft
.DS_Store
*.swp

# Claude Code local (per-machine) settings
.claude/settings.local.json

# graphify dependency-graph build output (curated subset is synced into the second brain)
graphify-out/

.mcp.json
.claude/settings.json
